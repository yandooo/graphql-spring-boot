type Query {
    post(id: ID!): Post
}

type Post {
    id: ID!
    date: String
    comments: [Comment!]!
}

type Comment {
    id: ID!
    description: String!
}
input PostInput {
    date: String
}
type Mutation {
    createPost(post:PostInput!):Post!
}